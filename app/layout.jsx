import './globals.css';

export const metadata = {
  title: 'VR Interativo - Quest 3 + PC',
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
